## Palette's Journal

## 2024-05-22 - Upgrading Interactive Elements
**Learning:** Replacing `div` with semantic `<button>` elements instantly improves keyboard accessibility but requires careful verification of CSS display properties (like `inline-block`) and focus states to match original designs.
**Action:** Always check `:focus-visible` styles when converting non-semantic elements to buttons to ensure keyboard users have clear visual feedback.
